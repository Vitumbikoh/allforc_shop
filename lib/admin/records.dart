import 'package:flutter/material.dart';

class ViewSalesRecordsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('View Sales and Refill Records'),
      ),
      body: Center(
        child: Text('View Sales Records Page'),
      ),
    );
  }
}
