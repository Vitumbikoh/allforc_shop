import 'package:flutter/material.dart';

class FinancialAccountingPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Financial and Accounting'),
      ),
      body: Center(
        child: Text('Financial and Accounting Page'),
      ),
    );
  }
}
